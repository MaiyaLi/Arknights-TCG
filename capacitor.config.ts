import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.arknights.tacticalsync',
  appName: 'Arknights: Tactical Sync',
  webDir: 'dist'
};

export default config;
